# Changelog

Формат — [Keep a Changelog](https://keepachangelog.com/ru/), версии — [SemVer](https://semver.org/lang/ru/).
Записи дублируются в приложении: после обновления показывается «Что нового» (`lib/data/changelog.dart`).

## [1.13.0] — 2026-08-30

### Добавлено
- QR-код приглашения (ADR-0016): в диалоге «Мой код» рядом с текстом появился QR — близкому проще снять его телефоном, чем пересылать строку. Кодирует пакет `qr`, рисуем сами `CustomPainter`'ом, как и остальную графику

### Исправлено
- Круговая диаграмма читается при мелких долях: доля в один процент — это дуга в 3,6°, которая превращалась в невидимую царапину. Теперь у каждого сектора есть минимальная заметная дуга, а крупные ужимаются пропорционально; зазор между секторами больше не съедает мелкие

## [1.12.2] — 2026-08-30

### Исправлено
- macOS: обновление больше не может оставить без приложения. Раньше старый `Numo.app` удалялся до распаковки нового — если распаковка не удавалась, приложения не оставалось вовсе. Теперь новая версия сначала распаковывается рядом, потом занимает место старой, а при неудаче старая возвращается на место
- Проверка прав на папку установки работает на всех платформах, а не только на Windows: на macOS без прав на `/Applications` обновление молча не вставало
- Диалог «обновление не установилось» показывает последние строки лога подмены прямо в окне — причину видно сразу, файл искать не нужно
- Linux: результат копирования проверяется и попадает в лог

## [1.12.1] — 2026-08-30

### Исправлено
- «Обновление не установилось» больше не появляется на пустом месте: отметка о начатой установке снимается, если скачивание сорвалось, — раньше сорванная загрузка выглядела как неудачная установка
- Windows: подмену файлов делает robocopy вместо Copy-Item — он устойчивее на деревьях каталогов и повторяет попытки, если файл ещё занят; код возврата проверяется и попадает в лог, а перед копированием приложение даёт системе отпустить файлы
- Сообщение о неустановившемся обновлении стало диалогом с кнопкой «Показать лог» (`%TEMP%\numo-update.log`) — причину видно, а не приходится гадать

## [1.12.0] — 2026-08-30

### Добавлено
- Разделение трат по долям поверх общих счетов: у траты на общем счёте можно отметить, между кем она делится, а раздел «Общий счёт» показывает, кто кому сколько должен, и позволяет отметить долг погашенным. Взаимные траты сворачиваются: 500 туда и 300 обратно превращаются в один долг на 200
- Локальные напоминания (ADR-0015): о регулярном платеже — накануне в 10 утра, о перерасходе бюджета — при открытии приложения. Всё считает и показывает само устройство, сеть не задействована. Переключатель в настройках; на Windows и в вебе раздела нет — там плагина не существует

### Изменено
- Лента операций читает данные страницами из SQLite: фильтры, поиск и сортировка ушли в базу с индексами по дате, счёту и удалению, список подгружается по 50 записей при прокрутке. Раньше каждый кадр перебирал весь список целиком
- Поиск по заметкам получил отдельную нормализованную колонку: SQLite‑функция `lower()` понижает только латиницу, и «Пятёрочка» искалась бы с учётом регистра

## [1.11.0] — 2026-08-30

### Добавлено
- Уведомление о новой версии больше нельзя пропустить: на «Обзоре» висит баннер с кнопками «Обновить» и «Позже» (до следующего релиза), у «Настроек» в боковой панели появляется точка, а в футере и в настройках видно, какая версия установлена и какая доступна

### Исправлено
- Доступность: суммы операций читаются словами («Расход 350,50 ₽»), у графиков появились текстовые описания, прогресс бюджета проговаривается с процентами, а кнопки-иконки (месяцы в аналитике, удаление правила и участника, добавление операции) получили подписи. Дашборд и лента проходят встроенные проверки Flutter на контраст, размер целей нажатия и наличие подписей
- Диалоги владеют своими контроллерами полей: прежнее отложенное освобождение оставляло висящий таймер

### Изменено
- Экран импорта умеет открываться с уже разобранными строками — так его можно открыть с готовыми данными и проверить тестом

## [1.10.0] — 2026-08-30

### Добавлено
- Код приглашения для общего счёта: у каждого участника есть код вида `numo1:…`, который можно показать и скопировать. Близкий вставляет его у себя — и вы сразу видите друг друга по именам, не дожидаясь первой сверки папки. Доступ к операциям код не даёт: его по-прежнему даёт общая папка в облаке
- Участника всё так же можно завести вручную — «Ввести имя вручную» в том же диалоге

## [1.9.0] — 2026-08-30

### Добавлено
- Общие счета сверяются при возврате в приложение, а не только при запуске (не чаще раза в минуту)
- Общие счета: счёт можно пометить общим и вести его вместе с другим человеком (ADR-0014). Обмен идёт через папку в вашем облаке — каждый участник пишет свой файл, данные сливаются пооперационно по времени изменения, удаления переживают слияние. Раздел «Общий счёт»: папка обмена, своё имя, участники, ручная сверка
- В ленте операций видно, кто из участников внёс запись
- В аналитике появилась пометка, когда суммы пересчитаны по курсу ЦБ или когда для валюты курса не нашлось

### Исправлено
- Windows: автообновление больше не срывается на кириллице в пути. Подмену файлов делает PowerShell-скрипт вместо `.bat` — `cmd` читал файл в CP866, а путь вида `C:\Users\Павел\AppData\Local\Temp\…` записан в UTF-8, поэтому копирование молча не проходило. Ушёл и `timeout /T`, не работающий в отсоединённом процессе
- Windows: если папка установки недоступна для записи (Program Files), приложение говорит об этом до скачивания архива, а не после
- Обновление, которое не встало, больше не проходит незамеченным: при следующем запуске Numo сообщает об этом и предлагает страницу релиза; ход подмены пишется в `%TEMP%\numo-update.log`
- Обновление берёт файлы из вложенной папки архива, если она там есть
- Статистика месяца, бюджеты и инсайты больше не складывают доллары с рублями: суммы валютных счетов пересчитываются в рубли по курсу ЦБ
- Импорт выписки больше не теряет одинаковые операции: две одинаковые покупки в один день импортируются обе (раньше вторая молча считалась дублем)
- Пополнение цели со счёта в другой валюте пересчитывается по курсу, а не переводит «то же число» другой валюты
- Ошибки импорта бэкапа переведены: раньше на английском интерфейсе они показывались по-русски
- Время и даты в ленте операций форматируются по языку интерфейса, а не всегда по-русски
- Диалоги с полем ввода (цель, правило, бюджет, PIN) больше не освобождают контроллер до конца анимации закрытия — это давало исключение «TextEditingController was used after being disposed»

### Изменено
- Добавление, правка и удаление операции пишут одну строку вместо перезаписи всей таблицы; импорт выписки и регулярные операции вставляются пачкой
- Удаление операции стало мягким: строка остаётся «надгробием» и вычищается через 180 дней — иначе удаление не доезжает до второго участника общего счёта

## [1.8.0] — 2026-08-25

### Добавлено
- Встроенные категории «Переводы», «Вклады» (расходы) и «Пополнения» (доходы); существующим пользователям досоздаются автоматически. Банковские рубрики выписок мапятся на них: «Перевод на карту»/«Перевод по СБП» → Переводы, «Пополнение»/«Входящий перевод»/«Зачисление» → Пополнения, «Вклады и счета»/«Капитализация» → Вклады
- Журнал импортов на экране «Импорт»: список уже импортированных файлов (имя, дата, число операций) с пагинацией «Показать ещё» по 10; новая таблица в БД (схема v9)

### Исправлено
- Чёрный экран после импорта из раздела «Импорт» в боковой панели: экран снимал корневой маршрут навигатора; теперь возврат происходит только когда есть куда, иначе экран сбрасывается к выбору файла

## [1.7.0] — 2026-08-25

### Добавлено
- Импорт выписок сам распределяет операции по категориям из банковских рубрик описания («Супермаркеты · PYATEROCHKA…» → Продукты, «Автомобиль»/«Такси» → Транспорт и т.д.); пользовательские правила всегда приоритетнее
- Кнопка «Применить к существующим» на экране правил теперь видна и без правил и дополнительно раскладывает уже импортированные операции из «Прочего» по рубрикам банка
- API-ключ AI-разбора хранится в системном хранилище секретов (Keychain / Keystore / DPAPI / Secret Service, ADR-0013) с прозрачным фолбэком и переносом старого значения; раньше лежал открытым текстом

### Исправлено
- Уведомление о новой версии действительно приходит: проверка повторяется раз в час и при возврате к окну (раньше — один раз за сессию), кэш проверки сокращён с суток до часа (свежий релиз был невидим до завтра даже после перезапуска), а кнопка «Обновить» из уведомления самообновляет приложение вместо открытия страницы релиза
- macOS: первое окно открывается размером 1240×800 — с боковой панелью, а не мобильной раскладкой; размер и позиция окна запоминаются между запусками

### Изменено
- Добавление, правка и удаление операции пишут в базу только затронутые строки вместо перезаписи всей таблицы — заметно быстрее на многолетних историях

## [1.6.1] — 2026-08-24

### Исправлено
- Масштаб интерфейса теперь действительно работает: контент раскладывался в полном размере окна и после увеличения обрезался справа и снизу (жёсткие оконные констрейнты продавливали SizedBox внутри Transform). Масштабирование переведено на FittedBox; проверено скриншотами запущенного приложения при 130 %

## [1.6.0] — 2026-08-24

### Исправлено
- Импорт PDF-выписок СберБанка: работает с настоящими выписками «по счёту карты» (проверено на реальном файле — все 63 операции, итог сходится копейка в копейку). Извлечение текста переписано: перекодировка по шрифтам страницы (несколько ToUnicode CMap), объекты в /ObjStm, экранированные скобки, позиционирование через Tm

### Добавлено
- Импорт из сбер-PDF переносит категорию банка и продавца в описание операции («Супермаркеты · PYATEROCHKA…»), приходы распознаются автоматически
- Регулярные платежи: день месяца выбирается календарной сеткой вместо длинного списка

## [1.5.2] — 2026-08-24

### Исправлено
- macOS: автообновление теперь ставится в /Applications, даже если приложение запущено из «транслоцированной» Gatekeeper-копии (после скачивания браузером) — раньше обновление молча не применялось
- macOS: после подмены файлов снимается карантин — обновлённое приложение открывается без предупреждений
- Диалог «Не удалось обновиться» показывает настоящую причину; ход подмены пишется в лог (`/tmp/numo-update.log`, на Windows — `%TEMP%\numo-update.log`)

## [1.5.1] — 2026-08-24

### Исправлено
- Боковая панель больше не пропадает при увеличенном масштабе интерфейса и при запуске: адаптивная компоновка считается по реальной ширине окна
- Масштаб корректно применяется на всех экранах, включая экран блокировки

### Добавлено
- Отдельная настройка «Вход по биометрии» рядом с PIN: тестовая проверка при включении и понятная причина, если Touch ID / отпечаток недоступны
- macOS: команда установки через Терминал без предупреждений Gatekeeper (в описании релиза)

## [1.5.0] — 2026-08-24

### Добавлено
- Автообновление: приложение само скачивает новую версию, подменяет файлы и перезапускается (Windows/macOS/Linux); страница релиза осталась запасным вариантом
- Персонализация: масштаб интерфейса (компактный / стандартный / крупный / очень крупный)

### Изменено
- macOS: приложение больше не в App Sandbox (вне App Store он не нужен) — автообновление и папки работают без ограничений

## [1.4.0] — 2026-08-24

### Добавлено
- Официальные иконки банков и сервисов: Сбер, Т-Банк, Альфа, ВТБ, Газпромбанк, Райффайзен, Озон Банк, Яндекс, WB, МТС, Совкомбанк, Tether
- Персонализация: выбор акцентного цвета приложения (10 вариантов), отдельный блок в настройках
- Папка выписок: Numo при запуске сам находит новые файлы выписок и предлагает импорт

### Изменено
- Список валют сокращён до RUB/USD/EUR

## [1.3.0] — 2026-08-24

### Добавлено
- Выбор валюты прямо при вводе операции: сумма в чужой валюте конвертируется в валюту счёта по курсу ЦБ (исходная сумма сохраняется в заметке)
- Узнаваемые значки брендов: Сбер, Т-Банк, МТС, Озон, Совкомбанк, Яндекс отрисованы как настоящие лого

### Изменено
- Цель с привязанным счётом обновляется сама от баланса счёта — кнопка «Пополнить» осталась только у целей без счёта
- «Бюджеты» переименованы в «Лимиты»
- Убран лишний заголовок на экране AI-аналитики

## [1.2.0] — 2026-08-24

### Добавлено
- Вход по биометрии: Touch ID (macOS), Windows Hello, отпечаток/Face ID (Android/iOS) — как альтернатива PIN
- Брендовые иконки счетов: Сбер, Т-Банк, Альфа, ВТБ, Озон, Яндекс и другие
- Цель можно привязать к счёту: пополнение спрашивает счёт-источник и делает реальный перевод
- «Что нового»: после обновления приложение показывает изменения версии
- «Стереть все данные» в настройках — полный сброс с подтверждением
- Версия приложения в боковой панели

### Исправлено
- macOS: у приложения не было доступа к сети (sandbox) — не работали курсы, обновления и AI
- Windows-сборка с биометрией (несовместимость плагина с новым компилятором MSVC)

### Изменено
- Сумма операции показывается в валюте выбранного счёта
- «Разбор от Claude» → «AI-разбор», «Импорт CSV» → «Импорт выписок»
- Экран блокировки показывает столько точек, сколько цифр в вашем PIN
- Релизные сборки начинаются с чистого состояния — без демо-данных
- Графики перерисованы: подписи на подложках без наложений, градиентные бары, аккуратные отступы
- Горизонтальные списки (категории, счета) теперь листаются мышью на компьютере

## [1.1.0] — 2026-08-24

### Добавлено
- Типы счетов: карта, наличные, вклад, накопительный — с группировкой на экране счетов
- Провайдеры AI-разбора: Claude (Anthropic), Cloud.ru, локальные модели через LM Studio, свой OpenAI-совместимый endpoint
- Импорт PDF-выписок Сбербанка (собственный извлекатель текста из PDF)
- Проработка интеграции с банками (docs/bank-integrations.md)

### Исправлено
- macOS-сборки подписываются ad-hoc; в релиз добавлена инструкция первого запуска (Gatekeeper)

## [1.0.0] — 2026-08-24

Первый релиз: операции, категории, бюджеты, цели, счета с переводами и мультивалютностью (курсы ЦБ), регулярные платежи, импорт CSV/OFX/XLSX, автокатегории, AI-аналитика, PIN, синхронизация через папку облака, тёмная/светлая темы, русский и английский языки, сборки для Windows/macOS/Linux.
